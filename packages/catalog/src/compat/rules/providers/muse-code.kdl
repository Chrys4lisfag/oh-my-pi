// Muse Code subscriptions use the same Model API wire contract as direct Meta API keys.

provider "muse-code" {
	include-encrypted-reasoning #true
	supports-reasoning-effort #true
	clamp-output-to-model-max #true
	// Subscription tier bills per request; the compact edit prompt and raw
	// grammar custom tools cut the recurring tool-schema bytes. Same wire
	// contract as `meta`, so both directives stay opt-in here only.
	edit-prompt-variant "compact"
	apply-patch-tool-type "freeform"
	class "unknown" {
		thinking-efforts "minimal" "low" "medium" "high" "xhigh"
		thinking-mode "effort"
	}
}
